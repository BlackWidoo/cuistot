{
  "name": "cuistot",
  "version": "1.0.0",
  "description": "Cuistot — le réseau social des gourmands.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "verify": "node --check server.js && node --check db.js && node --check app.js && npm test"
  },
  "engines": {
    "node": ">=20.0.0 <22.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.24.1"
  }
}
